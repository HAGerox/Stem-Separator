{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Stem Separator window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "updater:default",
    "drag:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
